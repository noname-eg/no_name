# خطة مشروع No Name

## 0. التحديث الحالي

- تم إضافة حقل كود الخصم داخل صفحة السلة لتمكين تطبيق الخصم قبل إتمام الطلب.
- تم ربط الحقل مباشرة مع أكواد الخصم الموجودة داخل `localStorage` عبر `useStore()`.
- يتم التحقق من صحة الكود، تطبيق نسبة الخصم على المجموع الفرعي، وعرض قيمة الخصم وتحديث الإجمالي فوراً.
- تم دعم حالة إزالة الكوبون وظهور رسالة خطأ عند إدخال كود غير صالح.
- الهدف التالي هو توثيق هذا التحديث داخل خطة المشروع ومتابعة إعدادات النشر وواجهة لوحة الإدارة.

## 1. نبذة عن المشروع

`No Name / MODEST WEAR` متجر أزياء محتشمة بواجهة عربية وإنجليزية، مع دعم اتجاهي `RTL` و`LTR` وتبديل اللغة من شريط المتجر.

### المسارات العامة الحالية

- `/` الصفحة الرئيسية.
- `/shop` صفحة المنتجات والفلاتر والأقسام.
- `/product/:id` تفاصيل المنتج.
- `/cart` سلة المشتريات.
- `/about` صفحة About no name.
- `/shipping` صفحة Shipping & returns.
- `/contact` صفحة Contact us.
- `/admin` لوحة التحكم.

### المزايا الحالية

- عرض المنتجات حسب الأقسام: Sets، Blouses / shirts، Skirts / pants، Denims، Jackets، وDresses.
- فتح تفاصيل المنتج، إضافة المنتجات إلى السلة، تعديل الكمية، وحذف المنتجات.
- المفضلة والبحث وتبديل اللغة.
- إدارة المنتجات والأسعار والمخزون والصور والفيديوهات من لوحة التحكم.
- تعديل الإعلان العلوي، عنوان ووصف الواجهة الرئيسية، اللون الأساسي، وروابط التواصل الاجتماعي.
- تعديل عناوين ووصف أقسام الموقع.
- تعديل صفحات About وShipping & returns وContact us بالعربية والإنجليزية.
- التحكم في صور About وعددها صورتان: `image1` و`image2`.
- إنشاء وحذف أكواد الخصم.
- عرض ملخص المبيعات والطلبات وتنبيهات المخزون.
- رابط `Casper.Dev` القابل للضغط في تذييل الموقع.

> الوضع الحالي هو نموذج متجر يعمل من جهة الواجهة، وليس منصة تجارة إلكترونية مكتملة بخادم بيانات أو دفع فعلي.

## 2. لغة البرمجة والأدوات

### اللغات

- TypeScript وTSX للواجهة والخادم والملفات المشتركة.
- HTML وCSS.
- Tailwind CSS لتنسيق الواجهة.

### التقنيات والمكتبات

- React 18.
- React Router 6 للتنقل داخل SPA.
- Vite 8 للبناء والتطوير.
- Express 5 لخادم Node.js.
- Node.js 22 كهدف لخادم الإنتاج.
- Tailwind CSS 3 وPostCSS وAutoprefixer.
- Radix UI وLucide React لعناصر الواجهة والأيقونات.
- React Query لإدارة استعلامات الواجهة.
- React Hook Form وZod للنماذج والتحقق عند الحاجة.
- Recharts للرسوم والبيانات في لوحة التحكم.
- Vitest للاختبارات.
- PNPM 10.14 كمدير الحزم.

### الملفات الأساسية

- `client/App.tsx`: نقطة تشغيل الواجهة وتعريف مسارات React Router.
- `client/components/store/StoreLayout.tsx`: حالة المتجر، المنتجات، السلة، الإعدادات، اللغة، التذييل، والشريط العلوي.
- `client/pages/`: صفحات المتجر ولوحة التحكم.
- `client/pages/Admin.tsx`: إدارة المنتجات والمحتوى والصفحات والكوبونات والتقارير.
- `client/global.css`: الأنماط العامة وثيم Tailwind.
- `server/index.ts`: إعداد Express ومسارات API الحالية.
- `server/node-build.ts`: تشغيل خادم الإنتاج وتقديم ملفات SPA.
- `shared/api.ts`: الأنواع المشتركة بين الواجهة والخادم.
- `vite.config.ts`: إعداد تطوير وبناء الواجهة.
- `vite.config.server.ts`: إعداد بناء الخادم إلى `dist/server`.
- `netlify/functions/api.ts`: نقطة Netlify Functions الحالية.

## 3. التشغيل المحلي

تأكد من استخدام Node.js 22 وPNPM 10.14، ثم نفذ:

```bash
pnpm install
pnpm dev
pnpm typecheck
pnpm test
pnpm build
pnpm start
```

### الأوامر

- `pnpm dev`: تشغيل Vite مع Express في منفذ واحد.
- `pnpm typecheck`: فحص TypeScript.
- `pnpm test`: تشغيل اختبارات Vitest.
- `pnpm build`: بناء الواجهة والخادم.
- `pnpm start`: تشغيل نسخة الإنتاج من `dist/server/node-build.mjs`.

### المنافذ ومجلدات البناء

- منفذ التطوير الافتراضي: `8080`.
- منفذ خادم الإنتاج: يقرأ `PORT`، والافتراضي `3000`.
- ملفات الواجهة: `dist/spa`.
- ملفات الخادم: `dist/server`.
- هدف خادم الإنتاج: `node22`.

### متغيرات البيئة

- يقرأ الخادم المتغير `PING_MESSAGE` لمسار `/api/ping`.
- يمكن تحديد منفذ الإنتاج من خلال `PORT`.
- أي متغير يبدأ بـ `VITE_` يصل إلى المتصفح ويُعد معلومة عامة؛ لا تضع فيه كلمات مرور أو مفاتيح سرية.
- يجب حفظ القيم السرية في إعدادات مزود الاستضافة، وعدم رفع ملف `.env` إلى المستودع.

## 4. التخزين والبيانات الحالية

التطبيق الحالي يخزن البيانات في متصفح المستخدم باستخدام `localStorage`، لذلك البيانات ليست مشتركة بين الزوار والأجهزة ولا تمثل قاعدة بيانات إنتاجية.

### مفاتيح التخزين الحالية

- `no-name-products`: المنتجات وتعديلات لوحة التحكم.
- `no-name-settings`: الإعلان، إعدادات الواجهة، اللون الأساسي، وروابط التواصل.
- `no-name-sections`: إعدادات أقسام الموقع.
- `no-name-pages`: محتوى About وShipping & returns وContact us، بما في ذلك صور About والبريد المستلم.
- `no-name-coupons`: أكواد الخصم.
- `no-name-orders`: الطلبات الموجودة محلياً.
- `no-name-language` و`no-name-language-version`: اللغة الحالية.

### حدود الوضع الحالي

- السلة والمفضلة وحالة الاشتراك مؤقتة داخل جلسة المتصفح، ولا توجد حسابات مستخدمين.
- رفع الصور والفيديوهات من لوحة التحكم يحول الملفات إلى Data URLs ويحفظها في `localStorage`، وهذا محدود بحجم تخزين المتصفح وغير مناسب للملفات الكبيرة.
- لا توجد مصادقة أو صلاحيات حقيقية للوحة `/admin`؛ يجب عدم فتحها للعامة قبل إضافة نظام دخول وصلاحيات.
- لا توجد قاعدة بيانات مشتركة للمنتجات والمخزون والطلبات والكوبونات.
- الدفع والشحن وتحديث المخزون الحقيقي غير متصلة بعد.
- يوجد خادم Express مع مسارات تجريبية مثل `/api/ping` و`/api/demo`، لكنه لا يقدم بعد API تجارة إلكترونية كاملة.
- إعداد البريد المستلم موجود ضمن إعدادات Contact، لكن إرسال الرسائل يحتاج مزود بريد وخادم API حقيقي.

## 5. متطلبات الربط والاستضافة

### خيار Builder أو استضافة Node

- Node.js 22.
- أمر البناء: `pnpm run build`.
- مجلد ملفات الواجهة: `dist/spa`.
- مجلد ملفات الخادم: `dist/server`.
- أمر التشغيل: `pnpm start`.
- يجب اختبار خادم الإنتاج بعد البناء والتأكد من أن مسارات React Router المباشرة تعيد `index.html`.

### خيار Netlify

الإعداد الحالي في `netlify.toml` يحتوي على:

- أمر بناء الواجهة: `npm run build:client`.
- مجلد النشر: `dist/spa`.
- الدوال: `netlify/functions`.
- تحويل `/api/*` إلى Netlify Function.

قبل النشر على Netlify:

1. التأكد من أن بيئة البناء تحتوي على PNPM أو استخدام أمر بناء متوافق مع مدير الحزم المعتمد.
2. التأكد من أن `dist/spa` هو مجلد النشر الصحيح.
3. إضافة SPA fallback إلى `index.html` لمسارات `BrowserRouter` عند الحاجة، حتى تعمل الروابط المباشرة بعد إعادة التحميل.
4. اختبار مسارات `/api/*` مع Netlify Functions.
5. إضافة متغيرات البيئة من إعدادات Netlify، وعدم رفع `.env`.
6. اختبار أن أي منطق يحتاج Node أو أسراراً يعمل داخل Function أو خادم آمن، وليس داخل كود المتصفح.

### مزودات الخدمات المطلوبة للإصدار الحقيقي

- قاعدة بيانات مثل PostgreSQL أو خدمة مُدارة مناسبة.
- تخزين ملفات وصور وفيديو عبر Object Storage وCDN.
- مزود بريد لإرسال نماذج Contact ورسائل تأكيد الطلب.
- بوابة دفع تدعم مصر وطريقة الدفع المناسبة للمتجر.
- شركة شحن وربط لتحديث حالة الطلبات وتكلفة الشحن.
- نطاق مخصص، DNS، وSSL.
- خدمة مراقبة أخطاء وتحليلات، مع نسخ احتياطي دوري.

## 6. ما يلزم قبل النشر

### فحوص تقنية

- تشغيل `pnpm typecheck` بنجاح.
- تشغيل `pnpm test` بنجاح.
- تشغيل `pnpm build` بنجاح.
- تشغيل نسخة الإنتاج واختبار `pnpm start`.
- مراجعة إعدادات الخادم ومسار ملف التشغيل `dist/server/node-build.mjs`.
- مراجعة إعداد Netlify Functions وSPA fallback.

### فحوص وظيفية

- اختبار كل المسارات والروابط المباشرة بعد إعادة التحميل.
- اختبار فتح المنتج، الإضافة إلى السلة، تعديل الكمية، والحذف.
- اختبار البحث والفلاتر والأقسام والمنتجات المكررة.
- اختبار اللغة العربية والإنجليزية واتجاهي RTL وLTR.
- اختبار الهاتف والكمبيوتر والشاشات المتوسطة.
- اختبار إضافة وتعديل وحذف المنتجات والمخزون والصور والفيديوهات.
- اختبار إنشاء وحذف الكوبونات.
- اختبار تعديل صفحات About وShipping & returns وContact us، وتغيير صور About.
- اختبار روابط التواصل والتأكد من فتحها في الوجهة الصحيحة.
- اختبار نموذج Contact بعد ربط مزود بريد فعلي، إن كان النموذج مفعلاً في النسخة النهائية.

### الأمان والمحتوى

- إضافة مصادقة وصلاحيات قبل نشر `/admin`.
- عدم وضع مفاتيح API أو بيانات اتصال قاعدة البيانات في متغيرات `VITE_*`.
- نقل التحقق من البيانات والعمليات الحساسة إلى الخادم.
- استبدال صور وفيديوهات التجربة بروابط إنتاج ثابتة أو تخزين CDN.
- وضع سياسة خصوصية وشروط استخدام وسياسة شحن واستبدال واضحة.
- مراجعة العنوان و`favicon` وبيانات Open Graph وSEO.
- تغيير العنوان الافتراضي في `index.html` من `Hello world project` إلى عنوان المتجر.
- إعداد النسخ الاحتياطي، مراقبة الأخطاء، والتحليلات.

## 7. خطة التطوير

### المرحلة الأولى: تثبيت الأساس

- توحيد إعدادات البناء والنشر بين Builder وNetlify.
- إضافة SPA fallback واختبار كل مسارات React Router.
- إصلاح وإضافة اختبارات للمسارات والسلة والمنتج ونماذج الموقع.
- تحديث عنوان الصفحة وmetadata وSEO وfavicon.
- تحسين الوصولية، خصوصاً أسماء الأزرار، الحقول، التنقل بلوحة المفاتيح، والتباين.

### المرحلة الثانية: Backend حقيقي

- اختيار قاعدة بيانات وإنشاء جداول المنتجات، المستخدمين، الطلبات، المخزون، الكوبونات، وإعدادات المحتوى.
- إنشاء API آمن للمتجر ولوحة الإدارة.
- نقل البيانات من `localStorage` إلى قاعدة البيانات مع خطة ترحيل واضحة.
- إضافة التحقق من المدخلات على الخادم باستخدام مخططات Zod أو ما يعادلها.
- إضافة تسجيل دخول وصلاحيات منفصلة للإدارة.
- إضافة حماية للطلبات ومعدلات الاستخدام وتسجيل العمليات الحساسة.

### المرحلة الثالثة: التجارة الإلكترونية

- بناء Checkout حقيقي مع بيانات العميل والعنوان.
- ربط بوابة الدفع والشحن.
- إنشاء الطلبات وتحديث المخزون تلقائياً بعد نجاح الدفع أو تأكيد الطلب.
- إضافة حالات الطلب وإشعارات البريد.
- تفعيل نموذج Contact لإرسال الرسائل إلى البريد المحدد من لوحة التحكم.
- إضافة منع التكرار والتعامل الآمن مع فشل الدفع أو انقطاع الشبكة.

### المرحلة الرابعة: الوسائط والأداء

- نقل الصور والفيديوهات إلى تخزين خارجي وCDN بدلاً من Data URLs.
- ضغط الصور وإنشاء أحجام responsive واستخدام Lazy Loading.
- تحسين تقسيم JavaScript وتقليل حجم الحزم.
- تحسين Core Web Vitals وسرعة صفحات المنتجات.
- إضافة مراقبة للأخطاء والأداء والتنبيهات.

### المرحلة الخامسة: النمو والتحسين

- إضافة SEO متقدم وبيانات منظمة للمنتجات.
- إضافة تحليلات التحويل وسلوك العملاء مع مراعاة الخصوصية.
- حفظ المفضلة والسلة والحسابات على الخادم للمستخدمين المسجلين.
- إضافة تقارير مبيعات حقيقية ومقارنة أداء الأقسام والكوبونات.
- دعم الترجمة وإدارة المحتوى من لوحة التحكم بشكل أوسع.
- إجراء اختبارات وصولية ومتصفحات وأجهزة قبل كل إصدار.

## 8. قائمة النشر النهائية

- [ ] تحديد مزود الاستضافة النهائي.
- [ ] تحديد قاعدة البيانات والتخزين والبريد والدفع والشحن.
- [ ] ضبط متغيرات البيئة السرية في مزود الاستضافة.
- [ ] إضافة المصادقة والصلاحيات للوحة الإدارة.
- [ ] تشغيل `pnpm typecheck`.
- [ ] تشغيل `pnpm test`.
- [ ] تشغيل `pnpm build`.
- [ ] اختبار `pnpm start` أو دوال Netlify حسب بيئة النشر.
- [ ] اختبار المسارات المباشرة وSPA fallback.
- [ ] اختبار السلة والمنتجات والصفحات والنماذج وروابط التواصل.
- [ ] مراجعة SEO والعنوان وfavicon وOpen Graph.
- [ ] إعداد النطاق وDNS وSSL.
- [ ] تفعيل النسخ الاحتياطي ومراقبة الأخطاء والتحليلات.
- [ ] إعداد خطة رجوع سريعة عند فشل الإصدار.
