# خطة الجاهزية والإطلاق — المرجع الحالي

## 1. الحالة الحالية

المشروع يعمل كمتجر React + Express متصل بـ Supabase. المنطق الحساس للطلبات والأسعار والكوبونات والمخزون موجود على الخادم وقاعدة البيانات، وليس في المتصفح.

الحالة: **جاهز لتجربة تكامل Supabase والنشر التجريبي، ولم يعتمد بعد كإطلاق تجاري نهائي حتى تنجح اختبارات الإنتاج.**

## 2. التحقق المحلي الحالي

- `pnpm typecheck` ناجح.
- `pnpm test` ناجح.
- `pnpm build` ناجح للواجهة والخادم.
- `pnpm start` يشغل `dist/server/node-build.mjs`.
- المعاينة المحلية تعمل على خادم واحد للواجهة وExpress API.

نجاح هذه الأوامر لا يثبت نجاح اتصال Supabase أو إعداد الاستضافة؛ يجب تنفيذ اختبار خارجي فعلي.

## 3. وظائف لوحة التحكم

- تسجيل الدخول عبر Supabase Auth وملف `admin_profiles`.
- إضافة وتعديل وأرشفة المنتجات.
- تعديل الصور والفيديو والوصف والسعر والمخزون.
- إدارة variants بصيغة اللون والمقاس والكمية والحالة.
- منع تكرار تركيبة اللون والمقاس قبل الحفظ.
- تعديل الشحن وحد الشحن المجاني والرسائل والمحتوى.
- إنشاء وأرشفة الكوبونات.
- عرض الطلبات وتصفيتها وفتح تفاصيلها والإيصالات.
- تحديث حالة الطلب وحالة الدفع والملاحظات.
- تعديل المخزون مع تسجيل حركة في `inventory_movements`.

## 4. منطق الطلب الحالي

```text
المنتجات من Supabase
→ السلة في المتصفح
→ حساب السعر من الخادم
→ التحقق من الكوبون والشحن
→ رفع الإيصال إلى Storage عند الحاجة
→ إنشاء الطلب عبر RPC
→ خصم المخزون ذريًا
→ حفظ order_items واللون والمقاس
→ إرجاع رقم الطلب وaccess token
→ عرض Order Summary
```

القواعد الأساسية:

- لا يثق الخادم في السعر أو الإجمالي القادمين من المتصفح.
- يرفض الكمية غير المتاحة.
- يخصم variant المحدد فقط عند وجوده.
- يستخدم `Idempotency-Key` لمنع الطلبات المكررة.
- لا يعتمد إنشاء الطلب على فتح WhatsApp.
- لا يخزن إيصال الدفع بصيغة Base64 داخل الطلب.

## 5. إعداد Supabase

أنشئ مشروعًا جديدًا ثم شغّل migrations بالترتيب التالي:

```text
supabase/migrations/001_admin_security.sql
supabase/migrations/002_store_data.sql
supabase/migrations/003_auth_and_customer_access.sql
supabase/migrations/004_product_images.sql
supabase/migrations/005_order_access_and_receipts.sql
supabase/migrations/005a_store_media_variants.sql
supabase/migrations/006_variant_order_stock.sql
```

بعد ذلك أنشئ `.env` في جذر المشروع:

```env
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<new-server-only-key>
APP_ORIGIN=https://your-production-domain.example
NODE_ENV=production
```

يجب أن يكون `SUPABASE_URL` رابط المشروع فقط، بدون `/rest/v1/`.

## 6. إنشاء حساب الإدارة

بعد ضبط `.env`:

```bash
pnpm admin:create admin@example.com
```

أدخل كلمة مرور لا تقل عن 12 حرفًا. لا ترسل كلمة المرور داخل GitHub أو ملفات المشروع.

## 7. اختبار Supabase الخارجي

نفّذ الاختبارات التالية على مشروع Supabase فارغ:

- نجاح كل migration بالترتيب.
- ظهور المنتجات بعد الزرع أو الاستيراد.
- إنشاء وتعديل وأرشفة منتج من لوحة الإدارة.
- حفظ صورة وفيديو المنتج في Storage.
- حفظ variants متعددة مع رفض التكرار.
- إنشاء طلب بvariant وخصم مخزونه فقط.
- إنشاء طلب بدون variants وخصم `products.stock`.
- رفض الطلب عند تجاوز المخزون.
- إعادة إرسال نفس `Idempotency-Key` دون إنشاء طلب إضافي.
- رفع إيصال والتحقق من أن bucket الإيصالات خاص.
- فتح تفاصيل الطلب والإيصال من لوحة الإدارة.
- اختبار 401 و403 لمسارات الإدارة.
- التأكد من عدم ظهور `SUPABASE_SERVICE_ROLE_KEY` في حزمة العميل.

## 8. النشر

مرجع النشر الكامل موجود في:

```text
HOST.EXAMPLE.md
```

الاستضافة الموصى بها حاليًا لهذا المشروع هي Hostinger بخدمة Node.js، لأن المشروع يحتاج Express API ومتغيرات بيئة سرية. نشر الواجهة كملفات ثابتة فقط على Netlify لن يشغل APIs الخادم.

Netlify يحتاج Adapter/Functions وربطًا خاصًا لـ Express قبل أن يصبح خيارًا إنتاجيًا. لا تضف هذا الربط يدويًا إذا كان المطلوب الالتزام بنسخة الكود الحالية دون تغييرات برمجية.

## 9. الترحيل من localStorage

المفاتيح القديمة التي يدعمها التصدير:

```text
no-name-products
no-name-settings
no-name-sections
no-name-pages
no-name-coupons
no-name-orders
```

الخطوات:

1. افتح النسخة القديمة التي تحتوي على البيانات.
2. ادخل لوحة التحكم.
3. اضغط `Export legacy data`.
4. سجّل الدخول إلى النسخة المتصلة بـ Supabase.
5. اضغط `Import legacy data` واختر ملف JSON.
6. راجع نتيجة السجلات المقبولة والمرفوضة.
7. اختبر منتجًا وطلبًا قبل حذف أي نسخة قديمة.

## 10. الملفات ووظيفة كل ملف

- `plane.md`: الحالة الفنية والمسارات وواجهات API والملفات الأساسية.
- `plane 2.md`: خطة الجاهزية والاختبارات الخارجية وخطوات الإطلاق.
- `HOST.EXAMPLE.md`: مرجع GitHub وHostinger وNetlify ومتغيرات البيئة.
- `.env.example`: أسماء متغيرات البيئة فقط، وليس مكان الأسرار.
- `nonametoshare.workflow`: وصف سير العمل العام للمشروع، ولا يتم تحديثه ضمن هذه المرحلة لأن المطلوب الحالي يقتصر على ملفات Markdown.
- `supabase/migrations/*.sql`: إنشاء الجداول والدوال والسياسات وBuckets.
- `server/routes/store.ts`: منطق APIs المتجر والإدارة والطلبات.
- `server/auth.ts`: المصادقة والجلسات والصلاحيات.
- `client/pages/Admin.tsx`: واجهة التحكم بالمحتوى والمنتجات والطلبات.

## 11. البنود المفتوحة

- تنفيذ اختبار Supabase فعليًا على المشروع الجديد.
- ضبط النطاق النهائي و`APP_ORIGIN`.
- تدوير المفاتيح التي تم كشفها سابقًا.
- إعداد النسخ الاحتياطية والمراقبة.
- اختبار Netlify بعد توفير Adapter/Functions مناسب، أو اعتماد Hostinger كخيار الإنتاج.

## 12. مرجع هذا الملف

وظيفة `plane 2.md` هي تحويل الحالة الحالية إلى قائمة جاهزية قابلة للتنفيذ والاختبار قبل الإطلاق، مع توضيح ما تم وما لم يتم.
