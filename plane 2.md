# الخطة التنفيذية المحدثة

## التقييم

تم تجاوز مرحلة النموذج المحلي جزئيًا. المشروع يحتوي الآن على خادم Express متصل بـ Supabase وطبقة طلبات تجارية، لكن الإطلاق النهائي متوقف على إعداد مشروع Supabase فعلي واختبار حسابات العملاء والإنتاج.

## المنجز

### الاستقرار

- `pnpm typecheck` ناجح.
- `pnpm test` ناجح: 5 اختبارات.
- `pnpm build` ناجح للواجهة والخادم.
- أمر `pnpm start` متوافق مع `dist/server/node-build.mjs`.

### الإدارة

- جلسات الإدارة وهاش كلمات المرور تعمل من الخادم.
- المنتجات قابلة للإضافة والتعديل والأرشفة عبر APIs محمية.
- الإعدادات والأقسام والصفحات والكوبونات تحفظ عبر APIs محمية.
- الطلبات تظهر في لوحة الإدارة ويمكن تحديث حالتها.
- العمليات الإدارية تسجل في `audit_logs`.

### الطلبات

- checkout يرسل IDs المنتجات والكميات فقط تقريبًا، ولا يعتمد الخادم على السعر القادم من المتصفح.
- السعر والخصم والشحن والمخزون يعاد حسابها داخل SQL.
- `Idempotency-Key` يمنع تكرار الطلب.
- رفع الإيصال يتم إلى Storage خاص قبل إنشاء الطلب.
- ملخص الطلب يسترجع من الخادم بعد refresh.

### الترحيل

- زر تصدير البيانات القديمة موجود في لوحة التحكم.
- زر استيراد البيانات القديمة يرسل إلى `/api/admin/migrate`.
- المنتجات والكوبونات والبيانات العامة تستخدم upsert.
- الطلبات القديمة لا تستورد تلقائيًا.

## خطوات إعداد Supabase

1. أنشئ مشروع Supabase جديدًا.
2. شغّل الملف التالي من SQL Editor أو عبر Supabase CLI:

```text
supabase/migrations/001_admin_security.sql
```

3. أنشئ `.env` في جذر المشروع:

```env
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=<public-auth-key>
SUPABASE_SERVICE_ROLE_KEY=<new-server-only-key>
APP_ORIGIN=http://localhost:8080
NODE_ENV=development
```

4. أنشئ مديرًا:

```bash
pnpm admin:create <username>
```

5. اختبر:

```bash
pnpm typecheck
pnpm test
pnpm build
pnpm dev
```

## متطلبات الاختبار الخارجي

يجب تنفيذها على مشروع Supabase حقيقي، ولا يمكن اعتبارها ناجحة من خلال build فقط:

- تطبيق migration على مشروع فارغ.
- إدخال منتج وكوبون وإعدادات تجريبية.
- اختبار إنشاء طلب بكمية صحيحة وكمية أكبر من المخزون.
- اختبار إعادة إرسال نفس `Idempotency-Key`.
- اختبار رفع إيصال والتحقق من خصوصية bucket.
- اختبار غياب مفتاح الخدمة عن حزمة العميل.
- اختبار 401 و403 لمسارات الإدارة.
- اختبار Netlify Function على النطاق المنشور.

## البنود المفتوحة

- واجهة Supabase Auth للعملاء.
- سياسات RLS الخاصة بـ `customer_profiles` والطلبات حسب `auth.uid()`.
- دعم اختيار المقاس واللون كمخزون منفصل عبر `product_variants`.
- Signed URLs لعرض إيصالات الإدارة بدل الاحتفاظ بمسار خام في الواجهة.
- rate limiting مركزي وCSRF/Origin protection.
- اختبار الإنتاج ونسخ قاعدة البيانات الاحتياطية.
- تدوير مفتاح `service_role` القديم من Supabase Dashboard.

## قرار الاستضافة

الاستمرار على React + Express مناسب حاليًا. يمكن تشغيله على Netlify Functions للتجربة، أو تشغيل `dist/server/node-build.mjs` كخدمة Node على Hostinger لاحقًا، مع نفس متغيرات البيئة وعدم تكرار منطق الأعمال.
